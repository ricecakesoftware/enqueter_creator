final DateTime dateTimeMinimum = DateTime(1970, 1, 1);

enum DialogResult {
  OK,
  Cancel,
  Yes,
  No,
}

enum Gender {
  Male,
  Female,
  Unknown,
}

enum QuestionnaireStatus {
  Creating,
  Published,
}

enum ResponderStatus {
  NotAnswering,
  Answering,
  Finished,
}
